// Google OAuth client IDs — replace with your real IDs.
// For Expo managed workflow, fill `expoClientId` and `webClientId` for web builds.
export const GOOGLE_OAUTH = {
  expoClientId: "",
  iosClientId: "",
  androidClientId: "",
  webClientId: "",
};

// NOTE: To obtain client IDs, go to Google Cloud Console -> OAuth consent screen -> Credentials
// For development with Expo, follow: https://docs.expo.dev/guides/authentication/#google
